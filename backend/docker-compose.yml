services:
  api:
    build: .
    ports:
      - "5000:5000"
    environment:
      - JWT_SECRET=${JWT_SECRET:-}
      - TOKEN_HOURS=${TOKEN_HOURS:-2}
    volumes:
      - api_data:/data
    restart: unless-stopped

volumes:
  api_data:
